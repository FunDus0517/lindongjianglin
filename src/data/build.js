/**
 * 构建标记：界面上直接显示，用来判断"我现在看到的是哪一版"。
 * 每次发布请把 BUILD 改成发布时的日期时间（本地时间），并在提交信息里写清楚。
 * @module data/build
 */
export const BUILD = '2026-09-14 09:45';
export const VERSION = 'V3.1';
export const LABEL = `${VERSION} · 构建 ${BUILD}`;
