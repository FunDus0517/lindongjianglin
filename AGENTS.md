# 《凛冬降临》协作铁律（任何人 / 任何 AI 动手前必读）

> 这些是项目所有者（用户）明确下达的规则，**优先级高于任何设计稿、参考图、AI 的"最佳实践"**。
> 违反其中任何一条，都属于必须立刻停下并回报的事故。

## 一、美术与图片：未经同意不得改动
1. **用户提供的任何图片、设计稿、参考图、美术素材，一律只读。**
   不得裁剪、不得重新绘制、不得换色、不得压缩、不得改名、不得删除、不得移动。
2. **不得私自"优化"或"重做"用户的美术方向。** 觉得有问题就**先问**，不要先改。
3. 我（AI）产出的一切视觉稿都属于**提案**：必须**发给用户看、明确同意后**才能进入代码。
   **未获同意不得写任何相关代码。**
4. 已入库的参考图（`docs/UI参考-*.png`、`docs/design/*`）是**原始件**，是比对基准，永久保留。

## 二、每一次改动都必须"按用户要求来"
5. 改动范围**严格限定在用户这次说的内容**。不顺手改别的、不"顺便优化"、不自行扩大范围。
6. 用户的用词就是验收标准。**不替换用户的措辞、不改用户定下的数值、不动用户指定的布局。**
7. 需求不明确时：**问一句**，不要靠猜。允许一次问清，不允许猜着做。
8. 已实现的功能**不得因为新需求被静默丢弃**；确需取舍，必须先说明影响再动。

## 三、不许出现"半坏不坏"的状态
9. 每一次提交都必须是一个**完整可用的切片**：要么不做，要么做完（含测试 + 部署 + 验证）。
10. 测试必须全绿（当前 98 项）才能提交；**禁止为了让测试过而放松断言**。
    （发奖系统干扰精确断言时，用 `muteRewards(state)` 之类的隔离手段，不是改断言。）
11. 所有状态变化必须经过 `core/effects.js` 的 `applyOutcome`；不得为了界面好看而绕过结算、
    不得写死假数据、不得留下"看起来能用其实没接线"的字段。
12. 部署必须**验证到底**：外网拉到真实文件、关键词命中、构建号对上，才算完成一次发布。

## 四、必须遵守的工程约束（细节见 docs/AI-交接说明.md）
13. 零依赖、零构建：原生 ES 模块 + hash 路由 + localStorage，不引入框架/打包器/图标库。
14. 随机数推进量是平衡的一部分：**不要随意新增 `chance()` 调用**，触发条件尽量写成确定性规则。
15. 新增状态字段必须**同时**加到 `createState()` 与 `restore()`，否则旧存档会坏。
16. 每处设施在基地页必须保持"一行可点开的 `.item`"契约（UI 与审计测试依赖它）。

## 五、汇报纪律
17. 只报**验证过的结果**；没验证的写"未验证"。不夸大、不含糊。
18. 出问题第一句就说**根因**，不要先描述现象。
19. 每次开工前与收尾前，都先看一眼用户的 UU 传输目录 `D:\GameViewer\Download`，
    确认有没有新的文档/图片/需求。

_最后更新：2026-09-14（由用户当面下达美术与改动范围规则后写入）_
